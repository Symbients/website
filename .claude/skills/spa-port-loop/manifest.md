# SPA port manifest (rhizome-spa)

Target: `spa.html` — vertical-scroll layout, frozen hero+nav, section-scoped
readers. Source: `lexicon.html` (+ `symbients.html`). See sibling skills
`spa-port-source` / `spa-port-target`. The loop (`spa-port-loop`) advances the
topmost actionable row. Statuses: `todo` · `in-progress` · `changes-requested` · `done`.

| # | section | target id | source | status | blocked_by | notes / reviewer verdict |
|---|---------|-----------|--------|--------|------------|--------------------------|
| 0 | chrome / shell | (whole `spa.html` frame) | lexicon.html theme bootstrap + tokens + `#field` + `.hero`/`.hero-nav` + theme-toggle; nav becomes anchor-scroll + scroll-spy; `main.scroll` container | done | — | **PASS** (reviewed). spa.html created (~766 lines): fixed `.site-hero` z5, `.scroll` padding-top `--hero-h`, 7 `#sec-*` placeholders w/ `scroll-margin-top`, scroll-spy IO, ported field/terminal/theme IIFEs verbatim. Sections land at 460=heroH. |
| 1a | home / creature (define) | `#sec-home` | symbients.html define mode: creature (`extitutional-ascii.js`) + `.definition-block` + `.dir-tree`; **NEW first section** | done | 0 | **PASS** (reviewer, part of the symbients-split review, 10/10). NEW first section + `home` nav tab + TITLE "Home". Creature backdrop + full definition-block (verbatim) + dir-tree (6 rows + L-system sprouts). `#def-word` & dir-tree "see symbients" → scroll `#sec-symbients` (+render 0); dir-tree lexicon/events/writings/organics/tree → scroll `#sec-<view>` (old iframe `navigateParent`/`window.parent` GONE → in-page `scrollToSec`). Creature start/stop via IntersectionObserver. |
| 1b | symbients (show) | `#sec-symbients` | symbients.html show mode: 9 `SYMBIENTS` 3-col (organic/portrait/synthetic) + `familiar` (being-composer) + `wires` SVG + `portrait-frame` + nav (back/prev/next/counter/to-lexicon) | done | 0 | **PASS** (independent reviewer, symbients-split 10/10 w/ evidence). 9 SYMBIENTS byte-identical (+IMAGES/HREFS); prev/next cycles all 9 w/ wrap + counter "0n / 09" + portrait href; familiar + wires (24 nodes) + portrait-frame render; `#back`→scroll `#sec-home`, `#to-lexicon`→scroll `#sec-lexicon`; familiar start/stop+`render` via IO. Dropped vestigial `three` importmap + own theme-bootstrap; recolors via chrome `themechange`. Masthead "Symbients"; responsive 3→1 col, wires hide ≤900, **0 h-overflow at 375/768/1280**; no regressions; sources untouched. (canvas frame-sampling unverifiable under hidden-tab rAF throttle — test artifact, not a defect.) |
| 2 | narrative | `#sec-narrative` | lexicon.html 1502–1600 + module 3024–3293 | done | 0 | **PASS** (independent reviewer, runtime evidence). 4 `.nar-cell` movements + submast + both blockquotes byte-identical to source; reader converted shell-wide→section-scoped (`#sec-narrative.is-reading`): grid hides in place, live `.nar-art` relocates into reader media + keeps animating, prev/next 1–4 wrap, COLLAPSE + guarded Esc close, art returns to cell. Masthead title tracks "Narrative"; theme light/dark/amber OK; responsive 4→2→1 cols, reader stacks on mobile, no h-overflow; console clean; lexicon/symbients untouched. Validates the scoped-reader pattern for the remaining card sections. |
| 3 | lexicon | `#sec-lexicon` | lexicon.html 1602–1805 + reader IIFE 2498–2827 | done | 0 | **PASS** (independent reviewer, 9/9 w/ evidence). Biggest section: 11 `term-*` cards / 3 bands + `.axis-scale` + the `<footer>`/colophon, byte-identical to source except the required footer-anchor conversion (`#narrative`→`#sec-narrative`, `#events`→`#sec-events`, `#writings`→`#sec-writings`). Section-scoped reader shows the RICH `content()` (def + voices[`*`=substrate] + threads); media/download hidden (no href/img). **Thread cross-links** (`<a href="#term-…">`) open the linked term in-reader (preventDefault, no anchor jump). axis-scale + footer hide during reading. Reader idx fixed to clean "L·0n" (source's `/[A-Z]·\d+/` bled the `.ct` connection-count → extract col-top text node instead; events/writings unaffected — their `.ct` is a glyph). Masthead "Lexicon"; theme OK; responsive n4→2→1 / n3 reflow + axis-scale stacks, **0 h-overflow at 375/768/1280** incl. open reader; dispatcher additive (19 ids); no regressions. |
| 4 | events | `#sec-events` | lexicon.html 1808–1865 | done | 0 | **PASS** (independent reviewer, 8/8 w/ evidence). 3 `data-href` cards faithful (incl. avif/webp `<picture>`); **established the section-scoped lexicon-reader pattern** (shell-wide `.shell.reading` → `#sec-events.is-reading`): card click opens reader IN PLACE (does NOT navigate away), `data-href` surfaced as clickable `.reader-media` link, index-list nav (no prev/next arrows), typed `def+PLACEHOLDER` lorem (source-faithful), guarded Esc, fly ghost. Added shared `window.__spaOpenCard`/`__spaCardOpeners` registry (id→opener+scroll) for the tree to reuse. Masthead "Events"; theme OK; 1/2/3-col reflow + reader stacks on mobile; console clean; no organics-shuffle leak; narrative un-regressed. |
| 5 | writings | `#sec-writings` | lexicon.html 1868–1932 | done | 0 | **PASS** (independent reviewer, after fix). 5 cards / 2 bands faithful; image-free cards use `data-img` for reader media; wr-05 PDF: `content()` download branch shows the real def (no lorem) + visible `.reader-download` (correct href/`download` attr — not triggered). Reused the events section-scoped reader; registered into shared `__spaCardOpeners`. **Reviewer caught 1 blocker** (reading-state CSS was `#sec-events`-only → writings reader never opened) → fixed by generalizing to `.port-section.is-reading` (with `:has(.reader)` so narrative keeps its submast). Also fixed a shared 3px reader-index h-overflow at 375 (`overflow-x:clip`), verified `openOverflow:0`. No regression to events/narrative; `:has()` honored. |
| 6 | organics | `#sec-organics` | lexicon.html 1935–2172 | done | 0 | **PASS** (independent reviewer, 7/7 w/ evidence). 22 `org-*` cards faithful to source: 12 real (org-01..12, `data-href`+portrait+real def) + 10 lorem placeholders (org-13..22, no href/img — source-accurate, NOT dropped). Events-style section-scoped reader; `content()`=def as-is (no placeholder); portrait→href media; lorem cards' media hidden. **Reshuffle-on-scroll-into-view** verified: Fisher–Yates, initial shuffle on load, IntersectionObserver edge-trigger on not-visible→visible (3 distinct id-orders captured), NOT continuous (no reorder on in-section scroll), guarded during reading, index re-syncs to new order; exposes `window.__spaReshuffleOrganics`. Zero new CSS (reuses all). Masthead "Organics"; theme OK; n4→2→1 reflow, 0 h-overflow at 375/768/1280 incl. open reader; dispatcher 41 ids; no regressions. |
| 7 | tree | `#sec-tree` | lexicon.html 2175–2185 + builder 2829–3018 | done | 0,2,3,4,5,6 | **PASS** (independent reviewer, 7/7 w/ evidence). FINAL section. L-system sapling built from `main.scroll .port-section` (excl. sec-tree): 54 rows, root + section/band branches + 44 leaves w/ per-leaf SVG sprouts; **41 `data-open` leaves match `__spaCardOpeners` exactly**. Rewired `act()` from `__lexShowView`/`__lexOpen` → leaf `window.__spaOpenCard(id)` (scroll+open scoped reader), branch `scrollIntoView('#sec-'+goto)`; no `window.parent`. home/symbients/narrative = childless goto-leaves (no `.col`, faithful). Masthead "Tree" at bottom (bottom-of-page spy guard confirmed). Theme re-inks sprouts (currentColor); `.tree` `overflow-x:auto` → 0 page-overflow at 375/768/1280; no regressions. |

## Suggested order
0 (chrome) → 2 (narrative, validates the scoped-reader pattern) → 4 (events) →
5 (writings) → 3 (lexicon) → 6 (organics) → 1 (symbients landing) → 7 (tree).
The manifest table is ordered by section number, but the loop may pick by this
suggested order as long as `blocked_by` is satisfied (chrome first; tree last).

## Carry-over follow-ups (non-blocking, revisit during/after the noted section)
- **tree (#7)**: ✅ RESOLVED — with real tree content, the bottom-of-page scroll-spy guard was confirmed (masthead reads "Tree" at absolute bottom; `atBottom` guard picks the last section).
- **dead placeholder CSS (harmonize phase)**: now that all 8 sections are real, `.port-section.port-placeholder:nth-child(even)` + `.port-title` + `.port-status` are unused (reviewer confirmed zero visual effect). Safe to delete during the harmonize pass.
- **hero**: v1 freezes the full hero (`--hero-h: clamp(260px,46vh,460px)`, ↓ to `clamp(200px,42vh,300px)` ≤640px); a compact fixed bar is a later refinement (harmonize phase).
- **narrative (#2)**: reviewer flagged a first-interaction-only scroll drift (clicking a cell right after a cold `#sec-narrative` load can drift the viewport down into the empty Events placeholder; not reproducible once layout settles). Caused by layout-settling/scroll-anchoring of the empty placeholder sections ABOVE narrative, not the reader logic — **re-confirm once `#sec-symbients` is ported to a stable height**. Same root cause makes scroll-spy read "Symbients" inside the `responsive.html` iframes (top-level masthead tracks "Narrative" correctly).

## Log
- (init) manifest created; skills authored; scaffold not yet built.
- (iter 0) chrome/shell ported into `spa.html` by porter subagent; independent reviewer → PASS. Theme/field/terminal IIFEs verbatim; fixed hero + anchor-scroll + scroll-spy working; lexicon.html/symbients.html untouched.
- (chrome polish) masthead title now tracks the section in view (no more static "Lexicon"); scroll-spy `pick()` made deterministic + bottom-of-page guard. Added a fluid type scale (`--fs-*`) + a ≤640px breakpoint → typography is responsive. Created `responsive.html` dev harness (mobile/tablet/desktop iframes). Skills updated: mobile-responsive is now IN SCOPE and the reviewer validates all three resolutions. Next actionable: #2 narrative.
- (iter 2 / narrative) resumed an interrupted port: the narrative markup + section-scoped reader + lab-asset module were already in `spa.html`'s working tree; completed self-checks (4/4 cells match source, module parses, server 200, 5 lab assets on disk) and ran an independent reviewer with full runtime evidence → **PASS** on all 7 checklist items. Recorded two minor non-blocking follow-ups (first-interaction scroll drift + harness scroll-spy, both from the empty placeholder neighbors). Committed locally. Next actionable per suggested order: #4 events.
- (iter 8 / tree) FINAL section ported via porter subagent: the L-system sapling site-map, built from `main.scroll .port-section` instead of `.shell .view`, with `act()` rewired to the scroll model (leaf → shared `window.__spaOpenCard(id)` which scrolls+opens the scoped reader; branch → `scrollIntoView('#sec-'+goto)`; no more `__lexShowView`/`__lexOpen`/`window.parent`). Sprout/rail/mark rendering kept verbatim. Self-checks (placeholder gone, #site-tree present, 8 scripts parse, TITLES "Tree", server 200) + independent reviewer → **PASS 7/7** (54 rows, 41 leaves == `__spaCardOpeners`, leaf-opens-reader + branch-scrolls verified, bottom-of-page masthead "Tree" confirmed, 0 page-overflow all widths, no regressions). **🎉 PORT COMPLETE — all 8 content sections real and reviewed-PASS.** Committed locally. Loop done.
- (iter 7 / symbients SPLIT) the hardest section: ported symbients.html (1433 lines) as TWO stacked sections — `#sec-home` (define: creature + definition-block + dir-tree, NEW first section + `home` nav tab/TITLE) and `#sec-symbients` (show: 9 SYMBIENTS 3-col + familiar + wires + portrait-frame + nav). Dropped the vestigial `three` importmap + symbients.html's own theme-bootstrap (chrome provides them). `.stage[data-mode]` toggle → two always-rendered sections with creature/familiar start/stop driven by IntersectionObservers; all mode-transition controls (`def-word`, dir-tree, `#back`, `#to-lexicon`, `navigateParent`) rewritten from iframe `window.parent.__lexShowView` to in-page `scrollToSec(#sec-<view>)`. Self-checks (both sections + order, 9 SYMBIENTS, both modules parse, importmap gone, server 200) + independent reviewer → **PASS 10/10** (creature/familiar/wires/portrait render, prev/next cycles 9 w/ counter, scroll mappings correct, no window.parent, masthead Home/Symbients, 0 h-overflow at all widths, no regressions). Note: post-crash the dev server was restarted as `python3 -m http.server 8000` (live-server flaked, exit 144). Committed locally. Next: #7 tree (the last section).
- (iter 6 / organics) ported via porter subagent (events-style reader, zero new CSS). Source is 12 authored cards + 10 lorem placeholders — preserved faithfully (not invented). The defining reshuffle-on-visit became an IntersectionObserver edge-trigger (not-visible→visible) that Fisher–Yates-reorders the grid + re-syncs the reader index, guarded so it never reshuffles while a reader is open or on in-section scroll. Self-checks (22/12/12 counts, 6 scripts parse, server 200) + independent reviewer → **PASS 7/7** (reshuffle proven with 3 distinct id-orders; 0 h-overflow all widths; dispatcher 41 ids; no regressions). Committed locally. Next: #1 symbients (SPLIT → new first section #sec-home + #sec-symbients).
- (iter 5 / lexicon) biggest card section ported via porter subagent: 11 cards/3 bands + axis-scale + footer/colophon, byte-identical to source bar the footer-anchor conversion to `#sec-*`. Rich reader content (def+voices+threads), thread cross-links open the linked term in-reader, axis-scale+footer hide while reading. Proactively fixed a latent source idx-label quirk (the `.ct` connection-count digits bled into the naive regex → "L·014"; now extracts the col-top text node → clean "L·0n"; events/writings left as-is since their `.ct` is a glyph). Self-checks (11/11 cards, scripts parse, server 200) + independent reviewer → **PASS 9/9** (rich content, cross-links, footer scroll, 0 h-overflow at all 3 widths, dispatcher now 19 ids, no regressions). Committed locally. Next: #6 organics.
- (iter 4 / writings) ported via porter subagent (reused the events reader; added `.cols.n4`, `.k-core`, data-img reader media, and the wr-05 PDF download branch/affordance). Reviewer found 1 blocker: reading-state CSS was scoped `#sec-events`-only so the writings reader never displayed → **generalized the reading-state selectors to `.port-section.is-reading`** (with `.port-section.is-reading:has(.reader) .submast{display:none}` so narrative — which has `.narr-reader`, not `.reader` — keeps its submast). Also clipped a shared 3px `.reader-index` h-overflow at 375 (`overflow-x:clip`), verified `openOverflow:0`. Re-review → **PASS**, no regression to events/narrative. These two fixes are shared infra → lexicon/organics inherit them. Committed locally. Next: #3 lexicon.
- (iter 3 / events) ported via porter subagent; **established the reusable section-scoped lexicon-reader pattern** (the source's shell-wide `.shell.reading` reader → per-section `#sec-events.is-reading`, cards open the reader in place w/o navigating, `data-href`→`.reader-media` link, index-list nav, typed placeholder, guarded Esc, fly ghost) + a shared `window.__spaOpenCard`/`__spaCardOpeners` registry for the tree. Self-checks (3/3 cards, all inline scripts parse, server 200, sources untouched) then independent reviewer → **PASS 8/8** with runtime + responsive (1/2/3-col) evidence. Committed locally. Writings/lexicon/organics now reuse this pattern. Next: #5 writings.
